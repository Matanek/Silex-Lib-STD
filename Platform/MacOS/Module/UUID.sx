use Interop.C
use Interop.Boundary

let clock_gettime_nsec_np = C.function<func(uint32) uint>(
    library:Boundary.System,
    name:"clock_gettime_nsec_np"
)

func unix_milliseconds() int {
    let clock_realtime:uint32 = 0
    return (clock_gettime_nsec_np(clock_realtime) / 1_000_000) as int
}
