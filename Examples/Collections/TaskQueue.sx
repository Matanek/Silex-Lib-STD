use STD.Collections.Queue

func main() {
    var pending = Queue<str>()
    pending.add("compile")
    pending.add("test")
    pending.add("package")

    while task = pending.take() {
        print("Run: $(task)")
    }
}
